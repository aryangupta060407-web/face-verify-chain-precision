import React from "react";
import ReactDOM from "react-dom/client";
import VerifyApp from "./VerifyApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VerifyApp />
  </React.StrictMode>
);
